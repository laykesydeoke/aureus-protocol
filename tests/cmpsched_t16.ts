import{describe,it,expect}from"vitest";describe("cmpsched 16",()=>{it("case 16",()=>{const a=simnet.getAccounts();const d=a.get("deployer")\!;const r=simnet.callReadOnlyFn("yield-aggregator","get-total-deposits",[],d);expect(r.result).toBeDefined();});});
