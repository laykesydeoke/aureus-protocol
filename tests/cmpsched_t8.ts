import{describe,it,expect}from"vitest";describe("cmpsched 8",()=>{it("case 8",()=>{const a=simnet.getAccounts();const d=a.get("deployer")\!;const r=simnet.callReadOnlyFn("yield-aggregator","get-total-deposits",[],d);expect(r.result).toBeDefined();});});
